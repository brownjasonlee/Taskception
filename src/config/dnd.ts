// Delay before showing visual feedback for nesting a task inside another (making it a child)
export const DRAG_NEST_DELAY_MS = 2000;

// Delay before showing visual feedback for reordering at the same level (before/after)
export const DRAG_REORDER_DELAY_MS = 150;

// Legacy export for backwards compatibility
export const DRAG_DELAY_MS = DRAG_NEST_DELAY_MS;